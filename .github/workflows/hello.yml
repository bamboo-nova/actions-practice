name: Hello                       # ワークフロー名
on: push                          # イベント名
jobs:                             # ジョブの定義
  hello:                          # ジョブID
    runs-on: ubuntu-latest        # ランナー(Ubuntu)
    steps:                        # ステップの定義
      - run: echo "Hello, world"  # シェルコマンドの実行
      - uses: actions/checkout@3  # アクションの呼び出し
